include_relative_list(engine)
include_relative_list(file)
include_relative_list(system)
include_relative_list(effect)
#include_relative_list(spell)
include_relative_list(utils)
IF(V8LINK)
include_relative_list(script)
ENDIF(V8LINK)